import React from 'react';

/**
 * @description SandBox should be used during to testing to show how
 * everything would look in outlook.
 * @param {jsx} children
 * @returns {*}
 * @constructor
 */
const SandBox = ({children}) => <div className="sandbox">{children}</div>;

export default SandBox;